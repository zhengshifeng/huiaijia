package com.flf.controller.app;

import com.flf.service.HajCommodityPromotionAreaService;
import org.apache.log4j.Logger;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;

/**
 * 
 * <br>
 * <b>功能：</b>HajCommodityPromotionAreaAppController<br>
 * <br>
 */ 
@Controller
public class HajCommodityPromotionAreaAppController {
	
	private final static Logger log = Logger.getLogger(HajCommodityPromotionAreaAppController.class);
	
	@Autowired(required = false)
	private HajCommodityPromotionAreaService service;
}
