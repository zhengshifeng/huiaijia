package com.flf.controller.app;

import org.apache.log4j.Logger;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

import com.flf.service.HajCouponInfoService;

/**
 * 
 * <br>
 * <b>功能：</b>HajCouponInfoAppController<br>
 * <br>
 */
@Controller
@RequestMapping(value = "hajCouponInfoApp")
public class HajCouponInfoAppController {

	private final static Logger log = Logger.getLogger(HajCouponInfoAppController.class);

	@Autowired(required = false)
	private HajCouponInfoService service;

}
